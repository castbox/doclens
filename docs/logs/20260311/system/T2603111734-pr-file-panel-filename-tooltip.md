# 任务简报：PR 文件面板文件名 Tooltip

## 变更说明
- 在右侧 PR 文件面板的文件名区域增加 `Tooltip`，悬停时显示完整文件名。
- 为文件名所在的标题行补充 `minWidth` 与 `flex` 约束，保持长文件名仍按单行省略显示，不挤压右侧已读状态标签。

## 影响文件
- `src/features/reviews/ui/ReviewDrawer.tsx`

## 行为保持说明
- 未调整 PR 文件面板的数据加载、筛选、已读状态、点击打开文件等原有行为。
- 仅增强文件名信息展示方式，默认视觉结构保持不变。

## 验证记录
- 构建校验：`pnpm build`
- 结果：通过

## 风险与说明
- 当前 tooltip 为文件名文本悬停触发；触屏设备仍以原有列表点击交互为主，不额外改变移动端行为。
