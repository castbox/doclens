# 文档大纲全量展示修复简报

- 时间：2026-02-07 03:51
- 目标：文档大纲内容全部显示，不使用大纲内部滚动条。

## 修改内容

- 文件：`src/features/docs/ui/DocOutline.tsx`
- 调整：
  1. 移除大纲容器 `maxHeight` 与 `overflowY`，取消内部滚动条
  2. 大纲条目文字从单行截断改为可换行展示
  3. 增加断词样式，保证长标题可完整显示

## 验证

- 执行：`pnpm build`
- 结果：通过（编译、类型检查、静态页生成均成功）
