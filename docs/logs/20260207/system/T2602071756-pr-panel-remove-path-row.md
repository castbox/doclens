# 任务简报：PR 文件面板去掉路径第二行

## 1. 目标
- 调整右侧 PR 文件面板卡片次信息展示。
- 去掉“路径”这一行，仅保留“类别 + 创建时间”。

## 2. 变更内容
- 文件：`src/features/reviews/ui/ReviewDrawer.tsx`
- 调整点：
  - 删除 `secondary` 区域中的路径 `Typography`（`item.path`）。
  - 保留并复用“类别 / 创建”同一行展示。
  - 去掉元信息行顶部间距（移除 `mt={0.75}`）。

## 3. 行为影响
- 仅前端展示变更。
- 未修改 API、类型、数据库结构。
- 未修改 docs 只读与路径安全逻辑。

## 4. 验证记录
- 构建校验：`pnpm build`
- 结果：通过（Next.js build 成功，类型检查通过）。

## 5. 风险与回滚
- 风险：低。卡片信息更简洁，但不再直接展示完整路径。
- 回滚：恢复 `ReviewDrawer.tsx` 中路径 `Typography` 节点。
