# 搜索结果与详情页分离简报

- 时间：2026-02-07 04:26
- 目标：`/docs` 不再内联搜索结果，搜索与详情分离显示。

## 修改内容

- 文件：`src/features/docs/ui/DocsWorkspace.tsx`
- 改动：
  1. 移除 `/docs` 页面内联 `SearchPanel`
  2. 移除顶部“显示/隐藏搜索”切换按钮
  3. 保留“搜索页”入口按钮，统一跳转到独立 `/search`

## 效果

- `/docs` 专注文档详情与审阅工作区。
- `/search` 负责搜索输入与结果列表。
- 搜索结果点击后再进入 `/docs` 详情，避免同屏混合。

## 验证

- 执行：`pnpm build`
- 结果：通过（编译、类型检查、静态页生成均成功）
