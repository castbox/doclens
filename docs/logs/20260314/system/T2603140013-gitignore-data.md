# 本次变更简报

## 变更内容

- 在 `.gitignore` 中新增 `data/` 规则，忽略整个数据目录。

## 原因

- 当前仓库存在未跟踪目录 `data/`，需要避免本地运行数据进入版本控制。

## 影响范围

- Git 忽略规则变更，不影响运行时行为。

## 验证记录

- 执行 `git status --short`，确认 `data/` 不再显示为未跟踪目录。
- 执行 `pnpm build`，构建通过。

## 风险与说明

- `data/` 下新增文件将默认不再纳入版本控制；若后续需要提交其中个别文件，应单独添加反向忽略规则。
